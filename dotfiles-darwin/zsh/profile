# -*- mode: shell-script -*-

() {
  local dir
  for dir (
      /usr/local/opt/*/libexec/gnubin(N)
      ~/Applications/Emacs.app/Contents/MacOS/bin
  ); do
    [[ -d ${dir} ]] && path=(${dir} ${path})
  done
}
